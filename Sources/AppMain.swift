import SwiftUI

@main
struct FormApp: App {
    var body: some Scene {
        Window("Form 表单布局", id: "main") {
            ContentView()
        }
        .defaultSize(width: 500, height: 600)
    }
}
